from setuptools import setup, find_packages

setup(
    name="expense-tracker-api",
    version="0.1.0",
    packages=find_packages(),          # will find your `app/` package
    install_requires=[],               # (optional: list here if you want)
)
